<nav id="top-nav" class="navbar navbar-dark navbar-expand-lg">

    <a class="navbar-brand" href="{{ path('homepage') }}"><span class="fas fa-book-open mr-1"></span>davidictionary</a>

    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
    <button
            class="navbar-toggler"
            id="navbar-toggler"
            type="button"
            data-toggle="collapse"
            data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent"
            aria-expanded="false"
            aria-label="Toggle navigation"
    >
        <span class="navbar-toggler-icon"></span>
    </button>
    {% endif %}

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        {% if is_granted('ROLE_ADMIN') %}
            <div>
                <ul class="navbar-nav mr-auto">
                    <li class="nav-action">
                        <a class="nav-item" href="{{ path('dictionaryList') }}"><span class="fas fa-list-alt mr-1"></span>View All Entries</a>
                    </li>
                    <li class="nav-action">
                        <a class="nav-item" href="{{ path('create') }}"><span class="fas fa-plus-circle mr-1"></span>Create a New Entry</a>
                    </li>
                </ul>
            </div>
        {% endif %}

        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <div class="user-info">
                <ul class="navbar-nav mr-auto">
                    <li>
                        <div class="nav-item">
                            Logged in as <b>{{ app.user.username }}</b> &ndash; <a href="{{ path('app_logout') }}">Logout<span class="fas fa-hand-peace ml-1"></span></a>
                        </div>
                    </li>
                </ul>
            </div>
        {% endif %}
    </div>
</nav>
